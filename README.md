For The Run Program Without Errors:

   - composer install
   - php artisan migrate
   - php artisan db:seed
   - php artisan serve
   
For The Run Scheduled Works:
   
   -php artisan work:scheduled

# turkcellChallange
